{% extends '::base.html.twig' %}
{% block body %}
<h1>StorageLocation list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Title</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('storagelocation_show', { 'id': entity.id }) }}">{{ entity.title }}</a></td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('storagelocation_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('storagelocation_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('storagelocation_new') }}">
            Neue Aufbewahrung anlegen
        </a>
    </li>
    <li>
        <a href="{{ path('welcome') }}">Zur&uuml;ck zur Filmliste</a>
    </li>
</ul>
{% endblock %}